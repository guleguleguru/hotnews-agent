# 🔄 双仓库维护指南

## 📦 仓库说明

### 1. **原始仓库**（私有/个人使用）
- **路径**: `e:\mot\news agnet\`
- **用途**: 你的个人使用，包含真实配置和敏感信息
- **GitHub**: `guleguleguru/news-agent`（或你的原始仓库名）

### 2. **开源仓库**（公开）
- **路径**: `e:\mot\news agnet\hotnews-agent-open-source\`
- **用途**: 开源项目，不包含敏感信息
- **GitHub**: `guleguleguru/hotnews-agent`（新创建的开源仓库）

## 🚀 第一步：推送开源版本到 GitHub

### 1. 清理不需要的文件

在 `hotnews-agent-open-source` 目录下，删除以下文件夹（如果存在）：

```powershell
# Windows PowerShell
cd "e:\mot\news agnet\hotnews-agent-open-source"
Remove-Item -Recurse -Force extensions\hotnews-agent\history -ErrorAction SilentlyContinue
Remove-Item -Recurse -Force extensions\hotnews-agent\logs -ErrorAction SilentlyContinue
Remove-Item -Recurse -Force extensions\hotnews-agent\__pycache__ -ErrorAction SilentlyContinue
```

### 2. 初始化 Git 并推送

```bash
cd "e:\mot\news agnet\hotnews-agent-open-source"

# 初始化 Git
git init

# 添加所有文件
git add .

# 提交
git commit -m "Initial commit: HotNews Agent - AI-powered daily news digest system"

# 连接到 GitHub 仓库（你已经创建了 guleguleguru/hotnews-agent）
git remote add origin https://github.com/guleguleguru/hotnews-agent.git

# 推送到 GitHub
git branch -M main
git push -u origin main
```

**如果遇到认证问题**：
- 使用 Personal Access Token（PAT）作为密码
- 或使用 SSH：`git remote set-url origin git@github.com:guleguleguru/hotnews-agent.git`

### 3. 添加 README（如果还没有）

GitHub 提示你添加 README，但我们已经有了 `README.md`。如果推送后还是提示，可以：
- 刷新页面
- 或者手动编辑 README（点击 "Add a README" 按钮，然后粘贴我们准备好的内容）

## 🔄 第二步：双仓库维护策略

### 方案 A：手动同步（推荐，简单）

**工作流程：**

1. **在原始仓库开发新功能**
   ```bash
   cd "e:\mot\news agnet"
   # 修改代码...
   git add .
   git commit -m "feat: 新功能"
   git push  # 推送到原始仓库
   ```

2. **同步到开源仓库**
   ```bash
   # 复制更新的文件到开源版本
   # 手动复制或使用脚本
   
   cd "e:\mot\news agnet\hotnews-agent-open-source"
   git add .
   git commit -m "feat: 新功能"
   git push  # 推送到开源仓库
   ```

**优点：**
- ✅ 简单直接
- ✅ 可以控制哪些更改同步到开源版本
- ✅ 可以清理敏感信息

**缺点：**
- ❌ 需要手动操作
- ❌ 可能忘记同步某些更改

### 方案 B：使用脚本自动同步（推荐，高效）

创建一个同步脚本 `sync-to-open-source.ps1`：

```powershell
# sync-to-open-source.ps1
# 将原始仓库的更改同步到开源版本

$sourceDir = "e:\mot\news agnet"
$targetDir = "e:\mot\news agnet\hotnews-agent-open-source"

# 1. 复制源代码文件
Write-Host "复制源代码文件..."
Copy-Item "$sourceDir\extensions\hotnews-agent\*.py" "$targetDir\extensions\hotnews-agent\" -Force

# 2. 复制脚本文件
Write-Host "复制脚本文件..."
Copy-Item "$sourceDir\scripts\*.py" "$targetDir\scripts\" -Force

# 3. 复制测试文件
Write-Host "复制测试文件..."
Copy-Item "$sourceDir\tests\*.py" "$targetDir\tests\" -Force

# 4. 复制 GitHub Actions 配置
Write-Host "复制 GitHub Actions 配置..."
Copy-Item "$sourceDir\.github\workflows\daily.yml" "$targetDir\.github\workflows\daily.yml" -Force

# 5. 更新 requirements.txt（如果需要）
Copy-Item "$sourceDir\requirements.txt" "$targetDir\requirements.txt" -Force

# 6. 更新 LICENSE（如果需要）
Copy-Item "$sourceDir\LICENSE" "$targetDir\LICENSE" -Force

# 7. 清理不需要的文件
Write-Host "清理不需要的文件..."
Remove-Item "$targetDir\extensions\hotnews-agent\history" -Recurse -Force -ErrorAction SilentlyContinue
Remove-Item "$targetDir\extensions\hotnews-agent\logs" -Recurse -Force -ErrorAction SilentlyContinue
Remove-Item "$targetDir\extensions\hotnews-agent\__pycache__" -Recurse -Force -ErrorAction SilentlyContinue

Write-Host "✅ 同步完成！"
Write-Host "下一步："
Write-Host "  cd $targetDir"
Write-Host "  git add ."
Write-Host "  git commit -m '描述你的更改'"
Write-Host "  git push"
```

**使用方法：**

```powershell
# 在原始仓库目录运行
cd "e:\mot\news agnet"
.\sync-to-open-source.ps1

# 然后提交到开源仓库
cd hotnews-agent-open-source
git add .
git commit -m "feat: 新功能描述"
git push
```

### 方案 C：使用 Git Subtree（高级）

如果你想要更自动化的同步，可以使用 Git Subtree，但这需要更复杂的设置。

## 📝 维护最佳实践

### 1. 开发新功能时

```bash
# 在原始仓库开发
cd "e:\mot\news agnet"
# 修改代码、测试...

# 提交到原始仓库
git add .
git commit -m "feat: 新功能"
git push

# 同步到开源仓库（使用脚本或手动）
# 确保清理敏感信息
# 提交到开源仓库
cd hotnews-agent-open-source
git add .
git commit -m "feat: 新功能（开源版本）"
git push
```

### 2. 需要同步的文件

**总是同步：**
- ✅ `extensions/hotnews-agent/*.py` - 源代码
- ✅ `scripts/*.py` - 工具脚本
- ✅ `tests/*.py` - 测试文件
- ✅ `.github/workflows/daily.yml` - GitHub Actions
- ✅ `requirements.txt` - 依赖
- ✅ `LICENSE` - 许可证

**需要清理后同步：**
- ⚠️ `env.example` - 确保没有真实密钥
- ⚠️ `README.md` - 确保没有敏感信息

**不同步：**
- ❌ `.env` - 包含真实配置
- ❌ `history/` - 数据库文件
- ❌ `logs/` - 日志文件
- ❌ 个人分析文档

### 3. 提交信息规范

**原始仓库：**
```bash
git commit -m "feat: 添加新功能"
git commit -m "fix: 修复bug"
```

**开源仓库：**
```bash
git commit -m "feat: 添加新功能（开源版本）"
git commit -m "fix: 修复bug（开源版本）"
```

或者更详细：
```bash
git commit -m "feat: 添加标题相似度去重功能

- 实现基于文本相似度的去重算法
- 支持可配置的相似度阈值
- 优化去重性能"
```

## 🎯 推荐工作流程

### 日常开发

1. **在原始仓库开发**
   ```bash
   cd "e:\mot\news agnet"
   # 开发、测试...
   git add .
   git commit -m "feat: 新功能"
   git push
   ```

2. **定期同步到开源仓库**（每周或每次重要更新后）
   ```bash
   # 运行同步脚本
   .\sync-to-open-source.ps1
   
   # 或手动复制文件
   
   # 提交到开源仓库
   cd hotnews-agent-open-source
   git add .
   git commit -m "feat: 新功能"
   git push
   ```

### 处理 Issues 和 PR

- **开源仓库的 Issues**：在原始仓库修复，然后同步
- **开源仓库的 PR**：审查后合并到开源仓库，然后同步回原始仓库（如果需要）

## 🔒 安全注意事项

1. **永远不要推送 `.env` 文件**
2. **检查 `env.example` 没有真实密钥**
3. **检查代码中没有硬编码的密钥**
4. **检查日志文件没有敏感信息**
5. **使用 `.gitignore` 确保敏感文件不会被提交**

## 📋 检查清单

在每次同步到开源仓库前，确认：

- [ ] 所有 API 密钥已移除
- [ ] 所有真实邮箱地址已移除
- [ ] `.env` 文件不在开源版本中
- [ ] `history/` 和 `logs/` 文件夹已删除
- [ ] `__pycache__/` 文件夹已删除
- [ ] `env.example` 使用占位符
- [ ] `README.md` 没有敏感信息
- [ ] 代码中没有硬编码的配置

## 🚀 快速命令参考

```bash
# 原始仓库：开发并推送
cd "e:\mot\news agnet"
git add .
git commit -m "feat: 新功能"
git push

# 开源仓库：同步并推送
cd "e:\mot\news agnet\hotnews-agent-open-source"
# （先运行同步脚本或手动复制文件）
git add .
git commit -m "feat: 新功能"
git push
```

---

**推荐使用方案 B（脚本同步），既高效又能控制同步内容！** 🎯

