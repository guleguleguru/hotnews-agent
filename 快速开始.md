# 🚀 快速开始 - 推送到 GitHub

## 第一步：清理并初始化 Git

```powershell
# 进入开源版本目录
cd "e:\mot\news agnet\hotnews-agent-open-source"

# 初始化 Git
git init

# 添加所有文件
git add .

# 提交
git commit -m "Initial commit: HotNews Agent - AI-powered daily news digest system"
```

## 第二步：连接到 GitHub 并推送

```powershell
# 连接到你的 GitHub 仓库
git remote add origin https://github.com/guleguleguru/hotnews-agent.git

# 设置主分支
git branch -M main

# 推送到 GitHub
git push -u origin main
```

**如果遇到认证问题：**
- 使用 Personal Access Token（PAT）作为密码
- 或配置 SSH 密钥

## 第三步：配置 GitHub Secrets

在 GitHub 仓库设置中添加 Secrets（用于 GitHub Actions）：
- `OPENAI_API_KEY`
- `SMTP_USER`
- `SMTP_PASS`
- `MAIL_TO`

详细步骤请查看 `上传到GitHub指南.md`

